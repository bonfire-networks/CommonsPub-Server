query getFollowedCollections {
    followingCollections {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followed
        resources {
            id
        }
        followersCount
        followingCount
    }
}
