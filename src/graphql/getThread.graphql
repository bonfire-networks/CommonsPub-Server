query getThread($id: Int!) {
  comment(localId: $id) {
    replies {
      id
      localId
      content
      published
      inReplyTo {
        localId
      }
      # author {
      #   id
      #   icon
      #   name
      # }
    }
    id
    content
    published
  }
}