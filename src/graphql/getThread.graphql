query getThread($id: Int!) {
  comment(localId: $id) {
    replies {
      id
      localId
      content
      published
      # author {
      #   id
      #   icon
      #   name
      # }
    }
    id
    content
    published
  }
}