query getCommunityQuery($context: Int!) {
    community(local_id: $context) {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followersCount
        followingCount
        followers {
            localId
            name
            preferredUsername
            icon
            summary
        }
        comments {
          localId
          id
          content
          published
        }
        collections {
            id
            localId
            preferredUsername
            name
            summary
            icon
            followersCount
            followingCount
        }
    }
}
