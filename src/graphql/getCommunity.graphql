query getCommunityQuery($context: Int!) {
    community(local_id: $context) {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followersCount
        followingCount

        collections {
            id
            localId
            preferredUsername
            name
            summary
            icon
            followersCount
            followingCount
        }
    }
}
