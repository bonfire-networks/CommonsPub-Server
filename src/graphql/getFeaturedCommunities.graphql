query getFeaturedCommunities {
  one: community(local_id: 7) {
    id
    localId
    preferredUsername
    name
    summary
    icon
    members {totalCount}
    collections {totalCount}
    threads {
                totalCount
            }
    followed
  }
   two: community(localId: 15) {
    id
    localId
    preferredUsername
    name
    summary
    icon
    members {totalCount}
    threads {
                totalCount
            }
    collections {totalCount}
    followed
  }
   three: community(localId: 5774) {
    id
    localId
    preferredUsername
    name
    summary
    icon
    members {totalCount}
    threads {
                totalCount
            }
    collections {totalCount}
    followed
  }
   four: community(localId: 5030) {
    id
    localId
    preferredUsername
    name
    summary
    icon
    members {totalCount}
    threads {
                totalCount
            }
    collections {totalCount}
    followed
  }
   five: community(localId: 5018) {
    id
    localId
    preferredUsername
    name
    summary
    icon
    members {totalCount}
    threads {
                totalCount
            }
    collections {totalCount}
    followed
  }
}
