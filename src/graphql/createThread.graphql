input CommentInput {
  content: String!
}

mutation createThreadMutation($id: String!, $comment: CommentInput!) {
  createThread(contextLocalId: $id, comment: $comment) {
    id
     author {
       name
       icon
     }
    localId
    content
    replies {
      id
    }
  }
}
