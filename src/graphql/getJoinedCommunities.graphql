query getJoinedCommunitiesQuery {
    followingCommunities {
        id
        localId
        preferredUsername
        name
        followed
        summary
        icon
        followersCount
        followingCount
    }
}
