mutation loginMutation($email: String!, $password: String!) {
    createSession(email: $email, password: $password) {
        token
        me {
         name
         id
         summary
         preferredUsername
         location
         email
         icon
        }
    }
}
