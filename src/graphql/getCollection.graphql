query getCollection($id: Int!) {
  collection(localId: $id) {
    id
    localId
    icon
    name
    content
    followed
    followersCount
    summary
    communities {
      localId
      name
      followed
    }
    resources {
      id
      localId
      name
      summary
      url
      icon
    }
  }
}
