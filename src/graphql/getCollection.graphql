query getCollection($id: Int!) {
  collection(localId: $id) {
    id
    localId
    icon
    name
    content
    followed
    followersCount
    summary
    communities {
      localId
      id
      name
      followed
    }
    resources {
      id
      localId
      name
      summary
      url
      icon
    }
  }
}
