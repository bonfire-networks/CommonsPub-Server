query getCollection($id: Int!) {
  collection(localId: $id) {
    id
    localId
    icon
    name
    content
    followersCount
    summary
    communities {
      localId
      name
    }
    resources {
      id
      localId
      name
      summary
      url
      icon
    }
  }
}
