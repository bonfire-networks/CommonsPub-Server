input CommentInput {
  content: String!
}

mutation createReplyMutation($id: String!, $comment: CommentInput!) {
  createReply(inReplyToLocalId: $id, comment: $comment) {
    id
    localId
    content
    published
    author {
      name
    }
    content
  }
}
