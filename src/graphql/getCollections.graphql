query getCollectionsQuery {
    communities {
        localId
      collections {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followersCount
        followingCount
      }
    }
}
