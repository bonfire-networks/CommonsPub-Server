query getCollectionsQuery {
    communities {
        localId
      collections {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followed
        resources {
          id
        }
        followersCount
        followingCount
      }
    }
}
