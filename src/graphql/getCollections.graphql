query getCollectionsQuery($limit: Int, $end: Int) {
    collections(limit: $limit, after:$end) {
        pageInfo {
            startCursor
            endCursor
        }
        nodes {
            id
            localId
            preferredUsername
            name
            summary
            icon
            followed
            followers {
                totalCount
            }
            community {
                localId
            }
            resources {
                totalCount
            }
        }
    }
}
