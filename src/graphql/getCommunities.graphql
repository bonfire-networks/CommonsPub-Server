query getCommunitiesQuery {
    communities {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followersCount
        followingCount
        collectionsCount
        followed
    }
}
