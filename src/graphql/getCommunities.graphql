query getCommunitiesQuery($parentId: Int!) {
    collections(parentId: $parentId) {
        id
        localId
        preferredUsername
        name
        summary
        icon
        followersCount
        followingCount
    }
}
