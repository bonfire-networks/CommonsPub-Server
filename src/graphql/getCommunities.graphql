query getCommunitiesQuery($limit: Int) {
    communities(limit: $limit) {
        pageInfo {
            startCursor
            endCursor
        }
        nodes {
            id
            localId
            preferredUsername
            name
            summary
            icon
            followersCount
            followingCount
            collectionsCount
            followed
        }
    }
}
