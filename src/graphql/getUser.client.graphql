query getUserQuery {
    user @client {
        __typename
        isAuthenticated
        data {
            id
            name
            email
            preferredUsername
            location
            icon
            summary
        }
    }
}
