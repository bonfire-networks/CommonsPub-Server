query getUserQuery {
    user @client {
        __typename
        isAuthenticated
        # data {
        #     id
        #     name
        #     preferredUsername
        #     location
        #     icon
        #     summary
        # }
    }
}
