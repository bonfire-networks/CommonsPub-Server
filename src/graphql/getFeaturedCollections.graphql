query getFeaturedCollections(
  $one: Int!
  $two: Int!
  $three: Int!
  $four: Int!
  $five: Int!
  $six: Int!
  $seven: Int!
  $eight: Int!
  $nine: Int!
  $ten: Int!
  ) {
  one: collection(localId: $one) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
   two: collection(localId: $two) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  three: collection(localId: $three) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
   four: collection(localId: $four) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  five: collection(localId: $five) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  six: collection(localId: $six) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  seven: collection(localId: $seven) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  eight: collection(localId: $eight) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  nine: collection(localId: $nine) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
  ten: collection(localId: $ten) {
    id
    localId
    name
    summary
    icon
    followers {totalCount}
    resources {totalCount}
    threads {totalCount}
    followed
  }
}
