query getCommentsQuery($id: Int!) {
    threads(contextLocalId: $id)  {
        localId
        content
        id
        published
        author {
            name
            id
            icon
        }
        replies {
          localId
            content
            id
            published
        }
    }
}
