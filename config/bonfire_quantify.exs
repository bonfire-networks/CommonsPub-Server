use Mix.Config

config :bonfire_quantify, :web_module, Bonfire.Web
config :bonfire_quantify, :repo_module, Bonfire.Repo
config :bonfire_quantify, :user_module, CommonsPub.Users.User
config :bonfire_quantify, :templates_path, "lib"
